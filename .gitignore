# Python build artefacts
__pycache__/
*.py[cod]
*.zip

# Editor / OS noise
.DS_Store
.idea/
.vscode/

# Kept locally, deliberately not published.
# Neither is needed to install or run the integration: HACS takes only
# custom_components/restart_guard/, and the release workflow zips only that.
#   tests/      - the standalone suites, run with: cd tests && python3 run_all.py
#   brand_src/  - SVG sources and the generator for the brand PNGs
tests/
brand_src/

__pycache__/
*.pyc
